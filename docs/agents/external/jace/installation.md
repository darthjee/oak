# Installation

Add `jace` to your `Gemfile`:

```ruby
gem 'jace'
```

Then run:

```bash
bundle install
```

Or install it directly:

```bash
gem install jace
```

---

[Back to hub](../jace-usage.md)
