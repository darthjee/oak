# Examples and summary index

Create `docs/agents/specs/photo/examples.md` with the snippets the other pages link to: `locals.php.sample`, the static photos/snaps rule, the uploads rule using `$storageRoot`, the `link_photos` CircleCI job, and the `release` `requires` change. Label them as proposals for the sub-issues.

Add one row per new page to `docs/agents/summary.md` (next to the Photo Upload entries), each with a one-line description that says the page is temporary (#328).

## Files to Change
- `docs/agents/specs/photo/examples.md`: new.
- `docs/agents/summary.md`: add the spec entries.
