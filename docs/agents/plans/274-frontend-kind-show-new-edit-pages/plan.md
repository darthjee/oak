# Plan: Frontend: Kind show/new/edit pages

Issue: [274-frontend-kind-show-new-edit-pages.md](../issues/274-frontend-kind-show-new-edit-pages.md)

## Overview

Add the missing `Kind` (show), `KindNew`, and `KindEdit` pages, mirroring the existing `Category`/`CategoryNew`/`CategoryEdit` page/controller/helper trio, and wire them into the router and `AppHelper.jsx`. All work is confined to `frontend/`.

See [frontend.md](frontend.md) for the full plan.
